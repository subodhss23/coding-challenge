// create a function that takes two strings and returns true if
// the first string ends with the second string; otherwise return false;

function checkEnding(str1, str2){
     console.log(str1.endsWith(str2));
}

checkEnding("abc", "bc");
checkEnding("abc", "d");
